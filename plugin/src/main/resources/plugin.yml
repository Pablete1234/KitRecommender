name: KitRecommender
description: ${description}
main: ${mainClass}
version: ${version} (git-${commitHash})
website: ${url}
depend: [PGM]

commands:
  kitdata:
    description: Show kit data for a player
    usage: /kitdata [player]
    permission: kitrecommender.kitdata
